require_relative "../lib/cell.rb"

module TicTacToe
  describe Cell do
    context "#initialize" do
      it "is initialized with a value of '' by default"
      it "can be initialized with an X"
    end
  end
end

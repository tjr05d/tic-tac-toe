require_relative "../lib/player.rb"

module TicTacToe
  describe Player do

    context "#initialize" do
      it "raises an exception when initialized with {}" 
      it "does not raise an error with a valid input hash"
    end

    context "#symbol" do
      it "returns the players symbol"
    end

    context "#name" do
      it "returns the players names"
    end

  end
end
